import { WithDirective } from './with.directive';

describe('WithDirective', () => {
  it('should create an instance', () => {
    const directive = new WithDirective();
    expect(directive).toBeTruthy();
  });
});
