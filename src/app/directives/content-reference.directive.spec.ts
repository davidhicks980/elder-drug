import { ContentReferenceDirective } from './content-reference.directive';

describe('ContentReferenceDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentReferenceDirective();
    expect(directive).toBeTruthy();
  });
});
