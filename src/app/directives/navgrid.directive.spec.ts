import { KeyGridDirective } from './keygrid.directive';

describe('NavgridDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyGridDirective();
    expect(directive).toBeTruthy();
  });
});
