import { ListKeyDirective } from './list-key.directive';

describe('ListKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new ListKeyDirective();
    expect(directive).toBeTruthy();
  });
});
